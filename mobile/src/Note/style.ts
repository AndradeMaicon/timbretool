import React from 'react';

import { StyleSheet } from 'react-native';

export const NoteStyle = StyleSheet.create({
  note: {
    fontSize: 280,
    fontWeight: '700',

    color: '#F0FFCE',
    marginTop: 30,
    marginBottom: 30
  }

})